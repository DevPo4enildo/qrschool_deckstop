<Window x:Class="qrschool_deckstop.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Авторизация - Инвентарный учет" Height="400" Width="500"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Вход в систему" FontSize="24" FontWeight="Bold" 
                   HorizontalAlignment="Center" Margin="0,0,0,30"/>

        <TextBlock Grid.Row="1" Text="Логин:" FontSize="14" Margin="0,0,0,5"/>
        <TextBox x:Name="txtUsername" Grid.Row="2" FontSize="14" Height="35"/>

        <TextBlock Grid.Row="3" Text="Пароль:" FontSize="14" Margin="0,10,0,5"/>
        <PasswordBox x:Name="txtPassword" Grid.Row="4" FontSize="14" Height="35"/>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,30,0,0" >
            <Button x:Name="btnLogin" Content="Войти" Width="100" Height="35" 
                    FontSize="14" Click="BtnLogin_Click"/>
            <Button x:Name="btnRegister" Content="Регистрация" Width="120" Height="35" 
                    FontSize="14" Click="BtnRegister_Click"/>
            <Button x:Name="btnExit" Content="Выход" Width="100" Height="35" 
                    FontSize="14" Click="BtnExit_Click" Background="#666"/>
        </StackPanel>

        <TextBlock x:Name="txtError" Grid.Row="6" Text="" Foreground="Red" 
                   HorizontalAlignment="Center" Margin="0,10,0,0"/>
    </Grid>
</Window>
